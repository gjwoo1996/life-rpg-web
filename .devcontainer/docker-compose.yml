services:
  workspace:
    # image: mcr.microsoft.com/devcontainers/base:ubuntu
    build:
      context: .
      dockerfile: Dockerfile
    command: sleep infinity
    env_file:
      - ../.env
    environment:
      # Common_Repo stack on external network gw-shared-devnet (postgres, ollama)
      - OLLAMA_HOST=http://ollama:11434
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/postgres
      - LIFERPG_LLM_MODEL_DEFAULT=qwen3:8b
      - LIFERPG_LLM_MODEL_FALLBACK=qwen2.5:7b
      - LIFERPG_LLM_MODEL_IMAGE=qwen2.5vl:7b
      - TZ=Asia/Seoul
    volumes:
      - ..:${containerWorkspaceFolder}:cached
    ports:
      - "3000:3000"   # Next.js
      - "3001:3001"   # NestJS API
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - default
      - shared-devnet

networks:
  shared-devnet:
    external: true
    name: gw-shared-devnet
